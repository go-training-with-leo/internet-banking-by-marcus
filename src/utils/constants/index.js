const StorageKey = {
  authAccessToken: '@auth:accessToken',
  authRefreshToken: '@auth:refreshToken',
};

const mainPagesRole = {
  ADMIN: '/employees',
  EMPLOYEE: '/accounts',
  CUSTOMER: '/cards',
};

export { StorageKey, mainPagesRole };
